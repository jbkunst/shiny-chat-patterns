---
title: Shiny + chat con ellmer
subtitle: Patrones progresivos para conectar conversación, datos e interfaz
author: LatinR 2026
date: "Joshua Kunst · noviembre de 2026"
lang: es
include-after-body: assets/cover.html
resources:
  - assets/**
filters:
  - shinylive
embed-resources: false
format:
  revealjs:
    theme: [default, assets/theme.scss]
    css: assets/slides.css
    width: 1600
    height: 900
    margin: 0
    center: false
    center-title-slide: false
    controls: false
    progress: true
    slide-number: false
    transition: fade
    background-transition: fade
    auto-stretch: false
    hash: true
    code-overflow: scroll
    code-block-height: 650px
    code-annotations: select
    highlight-style: github
revealjs-plugins:
  - codefrag
---

```{r}
#| label: setup-code-block
#| include: false
# Inserta un archivo R como Markdown. Las descripciones nombradas agregan
# anotaciones y pasos de resaltado para los rangos de líneas indicados.
code_block <- function(path, descriptions = NULL) {
  path <- file.path(dirname(knitr::current_input(dir = TRUE)), path)
  code <- readLines(path, encoding = "UTF-8", warn = FALSE)

  if (is.null(descriptions)) {
    return(cat("```r", paste(code, collapse = "\n"), "```", sep = "\n"))
  }

  ranges <- names(descriptions)
  first_lines <- as.integer(sub("-.*", "", ranges))
  code[first_lines] <- paste(code[first_lines], sprintf("# <%d>", seq_along(descriptions)))
  fence <- sprintf('```{.r code-line-numbers="|%s"}', paste(ranges, collapse = "|"))
  notes <- sprintf("%d. **%d.** %s", seq_along(descriptions), seq_along(descriptions), descriptions)
  cat(fence, paste(code, collapse = "\n"), "```", "", notes, sep = "\n")
}
```

## Una idea por vez

1. **Conservar.** Partimos siempre desde la misma aplicación.
2. **Agregar.** Incorporamos una sola capacidad en cada paso.
3. **Explicar.** Revisamos el cambio directamente en R.

## El recorrido

- **Shiny · app-00 a app-01.** Widgets, outputs y reactividad.
- **Chat + ellmer · app-02 a app-04.** Conversación, contexto y SQL.
- **Integración · app-05 a app-08.** Datos, interfaz, consultas y mapas.

## Shiny {.section-slide}

Primero, una interfaz reactiva con inputs, outputs y un estado que conecta ambos.

## 00 · Widgets y outputs

```{r}
#| echo: false
#| results: asis
code_block(
  "../app-00/app.R",
  c(
    "1-3" = "Carga las dependencias de la aplicación.",
    "8-19" = "Construye la interfaz con sidebar y gráfico.",
    "22-43" = "Conecta los inputs con los outputs reactivos."
  )
)
```

## 00 · La app

```{shinylive-r}
#| standalone: true
#| components: [viewer]
#| viewerHeight: 650
library(shiny)
library(bslib)
library(forecast)

# data --------------------------------------------------------------------
pasajeros <- data.frame(pasajeros = as.numeric(AirPassengers))

# user interface ----------------------------------------------------------
ui <- page_sidebar(
  title = "App 00 · Widgets y outputs",
  sidebar = sidebar(
    textInput("title", "Título", value = "Pasajeros aéreos"),
    sliderInput("n", "Cantidad de puntos", min = 24, max = 144, value = 48, step = 24),
    checkboxInput("forecast", "Mostrar pronóstico", value = FALSE)
  ),
  card(
    card_header(textOutput("plot_title")),
    plotOutput("plot")
  )
)

# server ------------------------------------------------------------------
server <- function(input, output, session) {
  output$plot_title <- renderText(paste0(input$title, " (", input$n, " observaciones)"))

  output$plot <- renderPlot({
    series <- ts(head(pasajeros$pasajeros, input$n), start = c(1949, 1), frequency = 12)

    if (input$forecast) {
      plot(
        forecast::forecast(series),
        col = "#0E4F5A", fcol = "#0E4F5A", shadecols = c("#DCECEE", "#A9CED2"),
        main = "", xlab = NULL, ylab = "Pasajeros (miles)"
      )
    } else {
      plot(
        series,
        col = "#0E4F5A", lwd = 2, main = "", xlab = NULL, ylab = "Pasajeros (miles)"
      )
    }
  })
}

shinyApp(ui, server)
```

## 00 a 01 · El cambio en el diff

<div class="code-diff" aria-label="Comparación entre app-00 y app-01">
<article>
<header>app-00/app.R</header>
<pre><code><span>sliderInput("n", ...)</span>
<span class="removed">- checkboxInput("forecast", ...)</span>
<span></span>
<span>series &lt;- head(pasajeros$pasajeros, input$n)</span>
<span class="removed">- plot(forecast::forecast(series), ...)</span></code></pre>
</article>
<article>
<header>app-01/app.R</header>
<pre><code><span>selectInput("continente", ...)</span>
<span class="added">+ textOutput("n_filas")</span>
<span></span>
<span class="added">+ data &lt;- reactive({ ... })</span>
<span class="added">+ output$n_filas &lt;- renderText(nrow(data()))</span></code></pre>
</article>
</div>

## 01 · Dashboard reactivo

:::: {.columns}
::: {.column width="36%"}
Los controles manuales comparten un único estado reactivo.

Todavía no hay chat. Este dashboard establece la línea base.
:::
::: {.column width="64%"}
```r
data <- reactive({
  if (input$continente == "Todos") paises
  else paises[paises$continente == input$continente, ]
})

output$n_filas <- renderText(nrow(data()))
```
:::
::::

## ellmer {.section-slide}

Después, una conversación con un modelo: cliente, prompt y respuesta.

## ellmer antes de Shiny

:::: {.columns}
::: {.column width="42%"}
<p class="pattern-label">Antes de la interfaz</p>

ellmer organiza la conversación con el modelo.

La clave se lee desde el entorno y nunca se escribe en el código.
:::
::: {.column width="58%"}
```r
library(ellmer)

chat <- chat_openai(
  model = "gpt-5-nano",
  system_prompt = paste(
    "Responde brevemente en español sobre los países de Gapminder en su último año.",
    "No inventes resultados numéricos."
  )
)

live_console(chat)
```
:::
::::

## Shiny + ellmer {.section-slide}

Conectamos conversación, datos reactivos y acciones de interfaz, una capacidad por vez.

## 02 · Chat sin acceso a los datos

:::: {.columns}
::: {.column width="36%"}
Agregamos conversación, pero mantenemos una frontera explícita.

El modelo responde sin conocer filtros ni valores visibles.
:::
::: {.column width="64%"}
```r
chat <- ellmer::chat_openai(
  system_prompt = "Ayuda a explorar la app."
)

observeEvent(input$chat_user_input, {
  stream <- chat$stream_async(input$chat_user_input)
  shinychat::chat_append("chat", stream)
})
```
:::
::::

## 03 · Una tool aporta contexto real

:::: {.columns}
::: {.column width="36%"}
El modelo consulta un resumen calculado desde los países visibles.

La tool lee el estado. Todavía no modifica el dashboard.
:::
::: {.column width="64%"}
```r
resumir_datos_visibles <- function() {
  df <- isolate(data())
  list(
    registros = nrow(df),
    poblacion_total = sum(df$poblacion),
    esperanza_promedio = round(mean(df$esperanza_de_vida), 1)
  )
}

chat$register_tool(tool(
  resumir_datos_visibles,
  "Resume los países visibles en el dashboard."
))
```
:::
::::

## 04 · SQL de solo lectura

:::: {.columns}
::: {.column width="36%"}
La tool acepta consultas acotadas y devuelve resultados verificables.

Solo `SELECT`, una tabla conocida y un límite de filas.
:::
::: {.column width="64%"}
```r
consultar_paises <- function(consulta) {
  DBI::dbGetQuery(con, validar_select(consulta))
}

chat$register_tool(tool(
  consultar_paises,
  "Ejecuta una consulta SELECT sobre la tabla paises.",
  arguments = list(
    consulta = type_string("Consulta SELECT sin punto y coma.")
  )
))
```
:::
::::

## 05 · La tool actualiza Shiny

:::: {.columns}
::: {.column width="36%"}
La conversación modifica el estado reactivo que alimenta el dashboard.

La validación ocurre antes de cambiar lo que ve la persona.
:::
::: {.column width="64%"}
```r
actualizar_dashboard <- function(consulta, titulo) {
  consulta <- validar_select(consulta)
  resultado <- DBI::dbGetQuery(con, consulta)

  consulta_actual(consulta)
  titulo_actual(titulo)

  list(mensaje = "Dashboard actualizado.", registros = nrow(resultado))
}
```
:::
::::

## 06 · Tools de interfaz

:::: {.columns}
::: {.column width="36%"}
Una tool abre una tabla o un gráfico sin recargar la aplicación.

El modal también entrega contexto de vuelta al modelo.
:::
::: {.column width="64%"}
```r
mostrar_tabla <- function(n, titulo) {
  n <- max(1L, min(as.integer(n), 100L))
  resultado <- head(isolate(data()), n)
  output$tabla_modal <- renderTable(resultado)

  showModal(modalDialog(
    title = titulo,
    tableOutput("tabla_modal")
  ))

  resultado
}
```
:::
::::

## 07 · Inputs tradicionales y chat

:::: {.columns}
::: {.column width="50%"}
### Inputs

Predecibles y visibles para preguntas conocidas.

```r
selectInput("continente", "Continente", continentes)
sliderInput("esperanza", "Esperanza de vida", 40, 85, c(60, 85))
```
:::
::: {.column width="50%"}
### Chat + query

Flexible para combinar condiciones difíciles de anticipar.

```r
consulta_actual <- reactiveVal(
  "SELECT * FROM paises"
)
```
:::
::::

## 08 · Tool, mapa y cámara

:::: {.columns}
::: {.column width="34%"}
El chat controla el dato y la perspectiva desde la que se observa.

La cámara se actualiza como proxy. El globo no se reconstruye.
:::
::: {.column width="66%"}
```r
ir_a_pais <- function(pais, zoom = 4) {
  ubicacion <- DBI::dbGetQuery(
    con,
    paste(
      "SELECT DISTINCT p.pais, g.longitud, g.latitud",
      "FROM paises p JOIN paises_geo g USING (codigo)",
      "WHERE lower(p.pais) = lower(?)"
    ),
    params = list(pais)
  )
  if (!nrow(ubicacion)) stop("No encontré ese país.")

  mapgl::maplibre_proxy("map", session) |>
    mapgl::fly_to(
      center = c(ubicacion$longitud[1], ubicacion$latitud[1]),
      zoom = zoom
    )

  paste("Mapa centrado en", ubicacion$pais[1])
}
```
:::
::::

## Demos posteriores

<div class="demo-slot">
  <p>Demo de Shiny, video corto o captura interactiva</p>
  <code>Reservado para una integración futura con Shinylive</code>
</div>

## Cierre {.section-slide}

La interfaz conserva su función y suma una nueva forma de expresar intención.
