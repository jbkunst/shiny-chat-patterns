---
title: Shiny + ellmer
subtitle: Patrones progresivos para conectar conversación, datos e interfaz
author: LatinR 2026
date: "Joshua Kunst · noviembre de 2026"
lang: es
include-after-body: assets/cover.html
resources:
  - assets/**
format:
  revealjs:
    theme: [default, assets/theme.scss]
    css: assets/slides.css
    width: 1600
    height: 900
    margin: 0
    center: false
    center-title-slide: false
    controls: false
    progress: true
    slide-number: false
    transition: fade
    background-transition: fade
    auto-stretch: false
    hash: true
    code-overflow: scroll
    code-block-height: 560px
    code-annotations: select
    highlight-style: github
    embed-resources: false
revealjs-plugins:
  - codefrag
---

```{r}
#| label: setup-code-block
#| include: false
# Inserta un archivo R como Markdown. Las descripciones nombradas agregan
# anotaciones y pasos de resaltado para los rangos de líneas indicados.
code_block <- function(path, descriptions = NULL) {
  path <- file.path(dirname(knitr::current_input(dir = TRUE)), path)
  code <- readLines(path, encoding = "UTF-8", warn = FALSE)

  if (is.null(descriptions)) {
    return(cat("```r", paste(code, collapse = "\n"), "```", sep = "\n"))
  }

  ranges <- names(descriptions)
  first_lines <- as.integer(sub("-.*", "", ranges))
  code[first_lines] <- paste(code[first_lines], sprintf("# <%d>", seq_along(descriptions)))
  fence <- sprintf('```{.r code-line-numbers="|%s"}', paste(ranges, collapse = "|"))
  notes <- sprintf("%d. **%d.** %s", seq_along(descriptions), seq_along(descriptions), descriptions)
  cat(fence, paste(code, collapse = "\n"), "```", "", notes, sep = "\n")
}
```

## Una idea por vez
<div class="cards" style="--cards: 3">
<article>
<small>01 · conservar</small>
<h3>La misma app</h3>
<p>Datos, estructura y comportamiento visibles cambian lo mínimo.</p>
</article>
<article aria-current="true">
<small>02 · agregar</small>
<h3>Un patrón</h3>
<p>Cada paso incorpora una capacidad que se reconoce en el diff.</p>
</article>
<article>
<small>03 · proyectar</small>
<h3>R de principio a fin</h3>
<p>El código visible coincide con las aplicaciones que usamos en el taller.</p>
</article>
</div>

## El recorrido
<p class="eyebrow">app-00 → app-08</p>

<div class="route" aria-label="Recorrido principal del taller">
<span>00<br>widgets</span>
<span>01<br>dashboard</span>
<span>02<br>chat</span>
<span>03<br>contexto</span>
<span>04<br>SQL</span>
<span>05<br>reactividad</span>
<span>06<br>interfaz</span>
<span>07<br>query</span>
<span>08<br>mapa</span>
</div>

## Shiny {.section-slide}
::: {.section-subtitle}
Primero, una interfaz reactiva: inputs, outputs y un estado que conecta ambos.
:::

## 00 · Widgets y outputs
<div class="placeholder">
<div>
Primera demo reservada para Shinylive
<code>app-00 · sin API key · integración al final</code>
</div>
</div>

## 00 → 01 · El cambio aparece en el diff
<div class="diff" aria-label="Comparación entre app-00 y app-01">
<section class="diff-pane">
<header>app-00/app.R</header>
<pre><code><span>sliderInput("n", ...)</span>
<span class="diff-line removed">- checkboxInput("forecast", ...)</span>
<span></span>
<span>series <- head(pasajeros$pasajeros, input$n)</span>
<span class="diff-line removed">- plot(forecast::forecast(series), ...)</span></code></pre>
</section>
<section class="diff-pane">
<header>app-01/app.R</header>
<pre><code><span>selectInput("continente", ...)</span>
<span class="diff-line added">+ textOutput("n_filas")</span>
<span></span>
<span class="diff-line added">+ data <- reactive({ ... })</span>
<span class="diff-line added">+ output$n_filas <- renderText(nrow(data()))</span></code></pre>
</section>
</div>

## 01 · Dashboard reactivo
:::: {.app-layout}
::: {.app-copy}
<p class="app-number">Patrón 01</p>
<p class="change">Los controles manuales comparten un único estado reactivo.</p>
<p class="keeps">Todavía no hay chat. El dashboard establece la línea base que conservaremos.</p>
:::
::: {.pattern-box}
<div class="language-pair"><span>R</span></div>

```r
data <- reactive({
  if (input$continente == "Todos") paises
  else paises[paises$continente == input$continente, ]
})

output$n_filas <- renderText(nrow(data()))
```

:::
::::

## ellmer {.section-slide}
::: {.section-subtitle}
Después, una conversación con un modelo: cliente, prompt y respuesta.
:::

## ellmer antes de Shiny
:::: {.app-layout}
::: {.app-copy}
<p class="app-number">Antes de la interfaz</p>
<p class="change">ellmer organiza la conversación con el modelo.</p>
<p class="keeps">La clave se lee desde el entorno; nunca se escribe en el código ni en las slides.</p>
:::
::: {.pattern-box}

```r
chat <- ellmer::chat_openai(
  system_prompt = "Ayuda a explorar los datos."
)

chat$chat("¿Qué puedes hacer?")
```

:::
::::

## Shiny + ellmer {.section-slide}
::: {.section-subtitle}
Ahora conectamos conversación, datos reactivos y acciones de interfaz, una capacidad por vez.
:::

## 02 · Chat sin acceso a los datos
:::: {.app-layout}
::: {.app-copy}
<p class="app-number">Patrón 02</p>
<p class="change">Agregamos conversación, pero mantenemos una frontera explícita.</p>
<p class="keeps">El modelo puede responder; aún no conoce los filtros ni los valores visibles.</p>
:::
::: {.pattern-box}
<div class="language-pair"><span>R</span></div>

```r
chat <- ellmer::chat_openai(
  system_prompt = "Ayuda a explorar la app."
)

observeEvent(input$chat_user_input, {
  stream <- chat$stream_async(input$chat_user_input)
  shinychat::chat_append("chat", stream)
})
```

:::
::::

## 03 · Una tool aporta contexto real
:::: {.app-layout}
::: {.app-copy}
<p class="app-number">Patrón 03</p>
<p class="change">El modelo consulta un resumen calculado desde los países visibles.</p>
<p class="keeps">La tool lee el estado; todavía no modifica el dashboard.</p>
:::
::: {.pattern-box}
<div class="language-pair"><span>R</span></div>

```r
resumir_datos_visibles <- function() {
  df <- isolate(data())
  list(
    registros = nrow(df),
    poblacion_total = sum(df$poblacion),
    esperanza_promedio = round(mean(df$esperanza_de_vida), 1)
  )
}

chat$register_tool(tool(
  resumir_datos_visibles,
  "Resume los países visibles en el dashboard."
))
```

:::
::::

## 04 · SQL de solo lectura
:::: {.app-layout}
::: {.app-copy}
<p class="app-number">Patrón 04</p>
<p class="change">La tool acepta consultas acotadas y devuelve resultados verificables.</p>
<p class="keeps">Solo <code>SELECT</code>, una tabla conocida y un límite de filas.</p>
:::
::: {.pattern-box}
<div class="language-pair"><span>R</span></div>

```r
consultar_paises <- function(consulta) {
  DBI::dbGetQuery(con, validar_select(consulta))
}

chat$register_tool(tool(
  consultar_paises,
  "Ejecuta una consulta SELECT sobre la tabla paises.",
  arguments = list(
    consulta = type_string("Consulta SELECT sin punto y coma.")
  )
))
```

:::
::::

## 05 · La tool actualiza Shiny
:::: {.app-layout}
::: {.app-copy}
<p class="app-number">Patrón 05</p>
<p class="change">La conversación deja de ser paralela y modifica el estado reactivo.</p>
<p class="keeps">La validación ocurre antes de cambiar lo que ve la persona.</p>
:::
::: {.pattern-box}
<div class="language-pair"><span>R</span></div>

```r
actualizar_dashboard <- function(consulta, titulo) {
  consulta <- validar_select(consulta)
  resultado <- DBI::dbGetQuery(con, consulta)

  consulta_actual(consulta)
  titulo_actual(titulo)

  list(mensaje = "Dashboard actualizado.", registros = nrow(resultado))
}
```

:::
::::

## 06 · Tools de interfaz
:::: {.app-layout}
::: {.app-copy}
<p class="app-number">Patrón 06</p>
<p class="change">Una tool puede abrir una tabla o un gráfico sin recargar la app.</p>
<p class="keeps">El modal es resultado de la conversación y también contexto para el modelo.</p>
:::
::: {.pattern-box}
<div class="language-pair"><span>R</span></div>

```r
mostrar_tabla <- function(n, titulo) {
  n <- max(1L, min(as.integer(n), 100L))
  resultado <- head(isolate(data()), n)
  output$tabla_modal <- renderTable(resultado)

  showModal(modalDialog(
    title = titulo,
    tableOutput("tabla_modal")
  ))

  resultado
}
```

:::
::::

## 07 · Inputs tradicionales vs consulta
<div class="app-layout">
<div class="app-copy">
<p class="app-number">Patrón 07</p>
<p class="change">Dos caminos expresan una intención y llegan al mismo resultado.</p>
<p class="keeps">Los filtros siguen siendo útiles; el chat amplía las consultas posibles.</p>
</div>
<div class="cards" style="--cards: 2">
<article>
<small>Camino A</small>
<h3>Inputs</h3>
<p>Predecible, visible y rápido para las preguntas conocidas.</p>
</article>
<article aria-current="true">
<small>Camino B</small>
<h3>Chat + query</h3>
<p>Flexible para combinar condiciones difíciles de anticipar.</p>
</article>
</div>
</div>

## 08 · Tool, mapa y cámara
:::: {.app-layout}
::: {.app-copy}
<p class="app-number">Patrón 08</p>
<p class="change">El chat controla el dato y también la perspectiva desde la que se observa.</p>
<p class="keeps">La cámara se actualiza como proxy: el globo existente no se reconstruye.</p>
:::
::: {.pattern-box}
<div class="language-pair"><span>R</span></div>

```r
ir_a_pais <- function(pais, zoom = 4) {
  ubicacion <- DBI::dbGetQuery(
    con,
    paste(
      "SELECT DISTINCT p.pais, g.longitud, g.latitud",
      "FROM paises p JOIN paises_geo g USING (codigo)",
      "WHERE lower(p.pais) = lower(?)"
    ),
    params = list(pais)
  )
  if (!nrow(ubicacion)) stop("No encontré ese país.")

  mapgl::maplibre_proxy("map", session) |>
    mapgl::fly_to(
      center = c(ubicacion$longitud[1], ubicacion$latitud[1]),
      zoom = zoom
    )

  paste("Mapa centrado en", ubicacion$pais[1])
}
```

:::
::::

## Espacio para las demos con API {.section-slide}
::: {.section-subtitle}
Las apps con modelos se incorporarán como demos desplegadas o grabadas. Ninguna clave se incluirá en el
navegador ni en el repositorio.
:::

## Slot reutilizable para una demo
<div class="placeholder">
<div>
Demo de Shiny, video corto o captura interactiva
<code>Reemplazar este bloque cuando exista un backend seguro</code>
</div>
</div>

## Cierre {.section-slide}
::: {.section-subtitle}
La interfaz no desaparece cuando llega el chat. Gana una nueva forma de expresar intención.
:::
